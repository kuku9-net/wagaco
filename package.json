{
    "devDependencies": {
        "sass": "^1.67.0"
    },
    "scripts": {
        "js": "esbuild src/index${FORMAT}.js --bundle --outfile=build${RELEASE}/index${FORMAT}.js --target=safari11",
        "css": "sass src/scss/style${FORMAT}.scss build${RELEASE}/style${FORMAT}.css",
        "pandoc": "./pandoc/build_pandoc $FORMAT"
    },
    "dependencies": {
        "@popperjs/core": "^2.11.8",
        "chart.js": "^4.4.0",
        "chartjs-plugin-annotation": "^3.0.1",
        "esbuild": "0.19.3",
        "glightbox": "^3.2.0",
        "tippy.js": "^6.3.7"
    }
}
